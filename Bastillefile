
ARG SSH_PORT

CP root /
CP etc /

RENDER /etc/rc.conf.local
RENDER /etc/ssh/sshd_config


CMD ssh-keygen -A
CMD ifconfig vnet0 inet6 accept_rtadv

PKG bootstrap
SERVICE sshd restart

PKG git vim mosh 

CMD ifconfig vnet0 | grep inet6
